# Teach session exports

Recorded teach-session traces saved here are copied into the user's config
folder on first launch when their config directory is empty. This is useful for
shipping sample sessions or for sharing recordings between users.
