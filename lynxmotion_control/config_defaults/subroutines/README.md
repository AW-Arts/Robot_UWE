# Subroutine defaults

Drop JSON subroutine recordings here (one file per routine) to ship starter paths
with the application. Files placed in this folder are copied to the user config
on first launch when their config directory is empty.
